export interface IAtores{
  nome: string;
  nascimento: string;
  idade: string;
  genero: string;
  nascenca: string;
  foto: string;
  pagina?: string; //** a ? indica que o campo nao é obrigatorio*/
  favorito: boolean;
}
